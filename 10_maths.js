console.log(Math);  // its a function

console.log(Math.abs(-5));  //eda negative re positive bania dei

console.log(Math.round(4.3)); // round of koira de points remove koira de

console.log(Math.round(4.6));   // 4.6 to taile 4.5 er thik ka beshi to 5+5 to 10 .. to beshi dom 5 .. thats why 5 answer 

console.log(Math.ceil(4.2)); // ceil mane  4 te ektu boro hoile oi eda 5 koira dibo means barai dibo

console.log(Math.floor(8855.54744788));   // point e aiye na converts to integer 

console.log(Math.min(1,2,3,4,5,6));  // min hoile array r modhe choto element ta dibo

console.log(Math.max(1,2,3,4,5,6)); //max hoile array r modhe boro element ta dibo

console.log(Math.random());   // random number generate koira dibo between 0 and 1

console.log(Math.random()*10); // 0 teke 10 r modhe random number aibo 

console.log((Math.random()*10)+1); // akn 0 aito na random number generate korle coz +1 increament hoice

console.log(Math.floor((Math.random()*10)+1));   // fully random  decimal numbers 

const min = 10 
const max = 20
console.log(Math.floor(Math.random() * (max - min  + 1 ) ) + min); // practice

// otp generator 

const large = 9999;
const small = 1111;

console.log(Math.floor(Math.random() * (large - small  + 1 ) ) + small); // practice
